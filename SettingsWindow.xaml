
<Window x:Class="GameDataEditor.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="180" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Data Folder Path:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Text="{Binding DataFolderPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="5,0" VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="Browse..." Command="{Binding BrowseFolderCommand}" Padding="10,2"/>
        </Grid>

        <CheckBox Grid.Row="1" Content="Expand all nodes by default" IsChecked="{Binding ExpandNodesByDefault}" VerticalAlignment="Center" Margin="0,10,0,0"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" IsDefault="True" Padding="10,2" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" IsCancel="True" Padding="10,2"/>
        </StackPanel>
    </Grid>
</Window>
